{
  "devDependencies": {
    "create-elm-app": "^4.2.6",
    "esbuild": "^0.7.1"
  },
  "scripts": {
    "build": "esbuild --minify --outdir=public/lib --target=es6 src/interpreter/*.ts; elm-app build",
    "start": "elm-app start"
  }
}
